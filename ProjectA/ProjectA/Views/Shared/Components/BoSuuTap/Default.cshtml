@model IEnumerable<BoSuuTap>

<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" id="boSuuTapDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Bộ sưu tập</a>
    <div class="dropdown-menu" aria-labelledby="boSuuTapDropdown">
        @foreach (var item in Model)
        {
            <a asp-controller="Home"
               asp-action="FilterByBoSuuTap"
               asp-route-id="@item.Id"
               asp-area="Customer"
               class="dropdown-item">@item.Name</a>
        }
    </div>
</li>
